apiVersion: apps/v1
kind: Deployment
metadata:
  name: minio-deployment
  labels:
    app: minio
spec:
  replicas: 1
  selector:
    matchLabels:
      app: minio
  template:
    metadata:
      labels:
        app: minio
    spec:
      containers:
<<<<<<< HEAD
        - name: minio
          image: minio/minio
          args:
            - server
            - /data
          envFrom:
          - secretRef:
              name: minio-secret
          ports:
            - containerPort: 9000
            - containerPort: 9001
          volumeMounts:
          - name: minio-data
            mountPath: /data
=======
      - name: minio
        image: minio/minio
        command:
        - /bin/bash
        - -c
        args:
        - minio server /data --console-address :9001
        envFrom:
        - secretRef:
            name: minio-secret
        ports:
        - containerPort: 9000
        - containerPort: 9001
        volumeMounts:
        - name: minio-data
          mountPath: /data
>>>>>>> 89aa74f1b52f146ce3db927d7eafffee7cbf4aa7
      volumes:
      - name: minio-data
        nfs:
          server: 192.168.1.111
          path: /mnt/tank/nfs
          readOnly: false

